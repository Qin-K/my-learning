<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="db.properties"/>

    <settings>
        <setting name="lazyLoadingEnabled" value="true"/>
    </settings>
    <!--配置别名-->
    <typeAliases>
        <!--<typeAlias type="com.demo.model.User" />-->
        <!--配置包名，指定包下的所有类都设置别名， 别名为类名第一个字母小写-->
        <package name="com.demo.model"></package>
        <package name="com.demo.vo"></package>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driverClass}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!--<mapper resource="com/demo/mapper/UserMapper.xml"/>-->
        <!--<mapper resource="com/demo/sqlmap/user.xml"/>-->
        <package name="com.demo.mapper"></package>
    </mappers>
</configuration>